class Solution:
    def findMaxConsecutiveOnes(self, nums: List[int]) -> int:
        mx = 0
        c = 0
        for i in range(len(nums)):
            if nums[i] == 1:
                c += 1
                if c >= mx:
                    mx = c
            else:
                c = 0
        return(mx)

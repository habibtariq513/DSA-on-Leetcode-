class Solution:
    def numIdenticalPairs(self, nums: List[int]) -> int:
        freq = {}
        pairs = 0

        for x in nums:
            pairs += freq.get(x, 0)
            freq[x] = freq.get(x, 0) + 1
        
        return pairs
